import SwiftUI

enum AppColors {
    static let normalMode = Color.green
    static let insertMode = Color.blue
    static let visualMode = Color.purple

    static let exerciseBorder = Color.accentColor
    static let completedGreen = Color.green
    static let tipBackground = Color.blue.opacity(0.08)
    static let importantBackground = Color.orange.opacity(0.08)
    static let codeBackground = Color.primary.opacity(0.04)
}
