# Canonical Q&A eval — 2026-04-23

**Total Q&As:** 200
**Flagged (any warning or fail):** 120

## Axis summary

| Axis | pass | warning | fail | n/a |
|---|---:|---:|---:|---:|
| vim_accuracy | 150 | 44 | 0 | 6 |
| kindavim_accuracy | 172 | 28 | 0 | 0 |
| terminalvim_accuracy | 52 | 5 | 0 | 143 |
| unsupported_consistency | 194 | 2 | 0 | 4 |
| token_conventions | 167 | 26 | 7 | 0 |
| verb_choice | 172 | 12 | 0 | 16 |
| related_quality | 141 | 50 | 9 | 0 |
| xref_validity | 177 | 5 | 18 | 0 |
| conciseness | 196 | 4 | 0 | 0 |

## Flagged entries

### `case-toggle` — How do I lowercase a selection?

- `u` in Visual mode is accurate for Vim, but Visual `u` is not explicitly listed in the kindaVim support corpus.
- Related includes `~ — toggle in visual`, which is not listed as a standalone supported command in the corpus.

**Failing:** xref_validity
**Warning:** kindavim_accuracy, related_quality
_The answer is factually good for Vim, but kindaVim support for Visual `u` is not explicitly attested in the corpus and one related xref is invalid._

### `case-toggle` — Can I use `~` with a count?

- Counted `~` behavior is correct for Vim, but the support corpus only attests bare `~`, not explicit count support.

**Warning:** kindavim_accuracy
_Stock Vim description is accurate; kindaVim count support for `~` is plausible but not explicitly documented in the corpus._

### `change-operator` — How do I change a whole line?

- Related summary for `S` says 'identical to cc'; in stock Vim they are synonyms, but kindaVim supports `S` without explicitly documenting preserved indent behavior.

**Warning:** related_quality
_The core explanation is accurate and supported, but the related note overstates equivalence a bit._

### `change-operator` — What's the difference between `cw` and `ciw`?

- Uses plain double quotes around foobar instead of backticks for command-like/token formatting example.

**Warning:** token_conventions
_The behavioral comparison between `cw` and `ciw` is correct, but the example formatting is slightly inconsistent with the token convention._

### `change-operator` — Does `cw` include the trailing whitespace?

- 'This is a quirk of the operator, not a bug' overstates intent; Vim help describes `cw` behavior as a special case rather than generally as an operator quirk.

**Warning:** vim_accuracy
_Core claim about `cw` leaving trailing whitespace is right, but the explanatory framing is a bit too loose._

### `change-operator` — How do I change to the next instance of a character?

- Related entry `dt` is broader than the answer's `ct{char}`/`cf{char}` focus and may be slightly underspecified compared with the answer's paired forms.

**Warning:** related_quality
_The explanation of `ct{char}` versus `cf{char}` is correct, though the related section could be a bit more parallel._

### `change-operator` — Is `s` the same as `cl`?

- Uses plain double quotes around change one character to the right instead of backticks or plain prose.

**Warning:** token_conventions
_Factually correct equivalence of `s` and `cl`, with only minor formatting inconsistency._

### `delete-operator` — What's the difference between `dd` and `D`?

- `D`/`d$` can delete across lines when given a count in Vim; answer omits that nuance.
- `cc` is not 'change entire line' in the strict sense of removing the newline; it changes whole lines and enters insert mode while preserving indent with autoindent.

**Warning:** vim_accuracy, related_quality
_Core distinction is correct for no-count use, but the wording is slightly oversimplified and one related summary is imprecise._

### `delete-operator` — How do I delete from the cursor to the end of the line?

- Related entry `d$ — explicit form of D` duplicates a command already named in the answer.

**Warning:** related_quality
_The answer itself is accurate, but the related list repeats a command already discussed._

### `delete-operator` — How do I delete multiple lines?

- Uses `Prefix` with multi-key sequence `dd`; convention prefers `Type` for multi-char sequences.
- Related entry `d{count}j` is not a concrete supported-corpus command token.
- Related `d{count}j` is awkward because counts are stripped and the canonical supported token is `dj`.

**Failing:** xref_validity
**Warning:** verb_choice, related_quality
_The answer is correct, but the verb convention and one related cross-reference need cleanup._

### `delete-operator` — How do I delete the character under the cursor?

- Related summary for `r` is misleading; `r` replaces the character under the cursor rather than 'without deleting' in a literal sense.

**Warning:** related_quality
_Main answer is solid, though one related summary is slightly misleading._

### `delete-operator` — What's the difference between `dw` and `de`?

- `dw` does not always delete trailing whitespace; with an operator, `w` has special end-of-line behavior, and whitespace handling depends on position/context.
- Saying `dw` removes 'the whole word unit' is an oversimplification of Vim's motion semantics.

**Warning:** vim_accuracy
_The teaching contrast is useful, but the stock-Vim behavior of `dw` is stated too absolutely._

### `delete-operator` — How do I delete from here to a specific character?

- Related examples `ct)` and `yt)` are example instantiations rather than canonical corpus tokens.
- Related items should usually use canonical command forms like `ct`/`yt` instead of specific-character examples.

**Warning:** related_quality, xref_validity
_The answer is accurate, but the related references are example-specific rather than canonical tokens._

### `delete-operator` — Where does deleted text go?

- In stock Vim, deletes and yanks both write the unnamed register, but deletes also affect numbered/small-delete registers; 'same place yanks go' is oversimplified.

**Warning:** vim_accuracy
_Accurate for kindaVim’s simplified model, but the stock Vim wording compresses important register distinctions._

### `delete-operator` — How do I delete without it being "deleted" — just remove it?

- Answer says kindaVim doesn't implement the black-hole register `"_`, but the unsupported feature itself is really the register prefix command, not the delete operator topic.
- Related summary uses `paste`, but canonical command wording elsewhere is usually `put` for `p`.

**Warning:** kindavim_accuracy, related_quality
_The unsupported claim and stock Vim description are basically right, but the item is slightly imprecise about what exact feature is unsupported._

### `delete-word` — How do I delete a word?

- `dw` does not always mean 'through the end of the next word'; in Vim it deletes over the `w` motion, which often lands at the start of the next word and can include following whitespace, with special end-of-line behavior.

**Warning:** vim_accuracy
_Good practical guidance, but the stock Vim description is a bit imprecise about what `dw` covers._

### `delete-word` — What's the difference between `dw` and `de`?

- `dw` vs `de` is described too absolutely; `dw` follows `w` motion semantics and its exact effect depends on cursor position and line endings, though the example intuition is reasonable.
- `daw` does not simply mean spaces on either side; it includes surrounding whitespace according to text-object rules, usually after the word, or before if needed.

**Warning:** vim_accuracy
_The contrast is directionally useful, but the stock Vim behavior is more conditional than stated._

### `delete-word` — How do I delete two words at once?

- Related command `d2e` is not in the support corpus; only canonical forms like `de` are listed.
- kindaVim support strips counts for lookup, so citing `d2e` as a related command is questionable in this schema.

**Failing:** xref_validity
**Warning:** kindavim_accuracy, related_quality
_Main answer is accurate, but the related entry uses a non-canonical token absent from the corpus._

### `delete-word` — Does `dw` work at the end of a line?

- Stock Vim behavior is oversimplified: `dw` at end of line depends on exact cursor position and special word-motion rules, not just 'no next word on the current line'
- kindaVim claim about typically staying within the line is not supported by the corpus

**Warning:** vim_accuracy, kindavim_accuracy
_Mostly helpful, but both the Vim explanation and the kindaVim-specific caveat are too loose to be fully reliable._

### `delete-word` — How do I delete just the letters of a word, not the surrounding spaces?

- Related includes `caw`, which is already mentioned in the answer; related items should not duplicate commands in the answer.

**Warning:** related_quality
_The answer is solid, but the related section duplicates a command already referenced in the answer._

### `dot-repeat` — What does `.` do?

- `<Esc>` should be backticked as a key token.
- Answer is 4 sentences; target is 1–3 sentences.

**Warning:** token_conventions, conciseness
_Content is correct, but it slightly exceeds the preferred length and misses backticks on `<Esc>`._

### `dot-repeat` — What's the "dot formula" for efficient edits?

- Related includes `n`, `/`, and `cw`, all of which are already mentioned in the answer.

**Warning:** related_quality
_The explanation is solid, but the related list is redundant with commands already named in the answer._

### `dot-repeat` — Can I combine `.` with a motion?

- Uses `CHANGE` in all caps instead of plain prose.
- Related includes `cw`, which already appears in the answer.

**Warning:** related_quality
_The content is correct, but there is a minor style issue and one related command duplicates the answer._

### `ex-commands` — Does kindaVim support `:s` (substitute)?

- `n` and `.` in the answer are not backtick-wrapped.
- Related includes `/`, `.`, and `cw`, all already mentioned in the answer.

**Warning:** token_conventions, related_quality
_The substitute guidance is accurate, but token formatting and related-command redundancy need cleanup._

### `ex-commands` — How do I run a shell command from kindaVim?

- `Terminal` in related is not a command token; only `Cmd+Space` is the supported token there.

**Warning:** token_conventions
_The unsupported explanation and stock Vim description are correct, though the related text mixes a valid shortcut with non-command prose._

### `ex-commands` — How do I open another file in kindaVim?

- Related command token `Cmd+O` is valid but summary `macOS open-file shortcut` does not match corpus wording `macOS open`.

**Failing:** xref_validity
**Warning:** related_quality
_The main answer is accurate, but the Related entry should use the corpus command summary verbatim or more closely._

### `ex-commands` — Does kindaVim support `:set` options?

- Related item is conceptual text, not a command token from the corpus.

**Warning:** xref_validity
_Accurately states that Ex-style `:set` is unsupported and gives a correct stock-Vim description._

### `ex-commands` — How do I yank into a specific register with `:reg`?

- Question conflates yanking to a register with `:reg`, which only displays registers in Vim.
- `terminalVim` says `:reg` lists every named register, but `:registers`/`:reg` lists numbered and named registers and more broadly displays register contents, not just named registers.

**Warning:** vim_accuracy, terminalvim_accuracy
_kindaVim non-support is stated correctly, but the stock-Vim framing around `:reg` is imprecise._

### `ex-commands` — How do I open help with `:help`?

- Shortcut `⌘⇧/` does not match the corpus token `Cmd+?` exactly.
- Answer references a host-app shortcut rather than a supported Vim command, which is fine contextually but slightly drifts from corpus wording.

**Warning:** kindavim_accuracy, token_conventions
_The unsupported `:help` claim is correct, but the shortcut notation should align better with the support corpus._

### `find-character` — Is `f` case-sensitive?

- Case-sensitivity for `f` is true, but the example `f)` vs `(` illustrates exact-character matching rather than letter case.
- Related summary says `/` is 'also case-sensitive by default', which depends on options in stock Vim and is not universally true.

**Warning:** vim_accuracy, related_quality
_The core answer is basically fine, but the example and related summary blur exact-match behavior with case sensitivity._

### `folds` — How do I collapse sections of my file?

- Related entry `Xcode code folding` is not a supported/unsupported kindaVim command token from the corpus.
- Related content is a host-app alternative, not a command reference entry.
- Answer uses plain `Xcode`/`BBEdit` names only; no command-token issue in answer, but related does not follow xref expectations.

**Failing:** xref_validity
**Warning:** token_conventions, related_quality
_The unsupported claim and terminal-Vim description are good, but the related reference is not a valid corpus command._

### `folds` — Does `za` do anything in kindaVim?

- Saying the `z` prefix is 'mostly unavailable' is too broad; kindaVim does support some `z` commands such as `zy`/`zp` in the corpus.
- Related command duplicates the command discussed in the answer: `za`.
- Related summary is accurate but redundant duplication should be avoided.

**Failing:** related_quality
**Warning:** vim_accuracy
_The main unsupported claim about `za` is correct, but the broader statement about `z` commands overreaches and the related link duplicates the answer command._

### `folds` — Can I hide parts of my file without folding?

- Related entry is not a kindaVim command reference.
- `host-app code folding` does not appear in the support corpus.

**Failing:** xref_validity
**Warning:** related_quality
_The unsupported answer and terminal Vim description are accurate, but the Related field should point to corpus commands/topics rather than free-form host-app text._

### `folds` — Does kindaVim support fold markers?

- Related command `:set foldmethod` is not listed in the support corpus; only `:` is listed, not this specific Ex form.

**Failing:** xref_validity
_The entry correctly says kindaVim lacks folding and accurately describes marker folds in Vim._

### `insert-mode-entry` — What does `s` do?

- `s` is defined in Vim as a synonym for `cl`, not `xi`; the effect is similar but the equivalence stated is not canonical.

**Warning:** vim_accuracy
_Mostly correct, but the stock Vim equivalence should be `cl` rather than `xi`._

### `insert-mode-entry` — What does `S` do?

- `S`/`cc` preserves the current line's indent when 'autoindent' is on, so 'enter insert on an empty line' is oversimplified for stock Vim.

**Warning:** vim_accuracy
_The main behavior is right, but the answer omits Vim's indent-preservation nuance for `S`/`cc`._

### `insert-mode-entry` — How do I add text without erasing what's there?

- Uses 'Any of' instead of the preferred imperative 'Type' for the multi-key list `i`, `I`, `a`, `A`, `o`, `O`.

**Warning:** verb_choice
_Behavior is accurate, but the phrasing doesn't follow the preferred Press/Type convention for command sequences._

### `insert-mode-entry` — My cursor disappeared after typing — am I still in insert mode?

- Related repeats `Esc`, which is already the command named in the answer.

**Warning:** related_quality
_The answer is fine, but the related command duplicates the main command instead of extending it._

### `insert-mode-entry` — How do I undo accidentally entering insert mode?

- 'Insert mode itself doesn't record as a change' is imprecise; what Vim tracks for undo is the text change, not mode entry alone.
- Uses 'then `u`' for a multi-step sequence; preferred wording would use 'Type' for `Esc` then `u`.

**Warning:** vim_accuracy, verb_choice
_The practical advice is fine, but the undo explanation is slightly imprecise and the verb choice is inconsistent with the style guide._

### `join-lines` — Does `J` remove all whitespace between the lines?

- In Vim, `J` does not always produce exactly one space; if line 1 has trailing whitespace or line 2 starts with `)`, spacing differs, and 'joinspaces' can produce two spaces after `.`, `!`, or `?`.
- Related summary says 'behavior varies in kindaVim' for `gJ`, but `gJ` is explicitly unsupported in the support corpus.
- The command token `gJ` in Related should be backtick-wrapped.

**Warning:** vim_accuracy, kindavim_accuracy, token_conventions
_This entry overstates `J` spacing behavior and its related note is inaccurate about kindaVim's support status for `gJ`._

### `join-lines` — How do I join multiple lines at once?

- Related uses `V3j`, which is not a canonical supported command token in the corpus.
- Related summary 'select lines and combine' is vague and doesn't clearly map to a supported command.

**Warning:** token_conventions, related_quality, xref_validity
_The main answer is correct for Vim and kindaVim, but the related entry is not well-formed against the corpus._

### `join-lines` — What happens if I join a line with an empty line?

- Saying `J` on a blank next line leaves a trailing space is generally right, but actual spacing depends on join rules and existing whitespace.

**Warning:** vim_accuracy
_This is close enough for teaching, but it overstates the exact spacing outcome._

### `join-lines` — How is `J` different from pressing Backspace at the start of the next line?

- 'without a position change' is inaccurate for `J` because it is a change command that leaves the cursor on the resulting joined line
- Backspace behavior at start of line is host/app-dependent and not a Vim command in {{normal}}
- kindaVim corpus supports `J` but does not document host-app option-dependent separator behavior

**Warning:** vim_accuracy, kindavim_accuracy
_The high-level contrast is understandable, but the specifics around cursor movement and host behavior are too shaky._

### `jumplist` — How do I jump back to where I was before a search?

- `Ctrl-O` and `Ctrl-I` should follow the app's backtick convention, e.g. `Ctrl-O`, `Ctrl-I` already do, but '/ — search' in Related is not backtick-wrapped.

**Warning:** token_conventions
_This unsupported entry is accurate, with only a minor token-format issue in Related._

### `jumplist` — Does `gd` go to the definition of a variable in kindaVim?

- Stock Vim description for `gd`/`gD` is oversimplified; `gd` is local-to-function and `gD` goes to global declaration, not merely first raw occurrence.
- Related entry is not a Vim/kindaVim command and does not appear in the support corpus.

**Failing:** related_quality, xref_validity
**Warning:** vim_accuracy
_kindaVim non-support is stated correctly, but the stock Vim explanation is imprecise and the related cross-reference is invalid._

### `jumplist` — How do I navigate back after jumping to a definition?

- `Ctrl-minus` in the answer is not backtick-wrapped.
- Related entry is not a command and does not appear in the support corpus.

**Failing:** related_quality, xref_validity
**Warning:** token_conventions
_The unsupported behavior and terminal Vim description are correct, but formatting and related-command quality are poor._

### `jumplist` — How do I jump to a tag in kindaVim?

- Saying kindaVim doesn't integrate with LSP is broader than the support corpus, which only establishes unsupported tag-related commands.
- Related entry is not a command and does not appear in the support corpus.

**Failing:** related_quality, xref_validity
**Warning:** kindavim_accuracy
_The core unsupported claim is fine, but it adds extra non-corpus claims and the related reference is invalid._

### `line-motions` — How do I jump to the start of a line?

- Mode should be written as `{{normal}}`, not `{{normal}}`-rendered here as single braces in source form if that's what the actual item uses; as given, `{{normal}}` is not present in the JSON text.

**Warning:** token_conventions
_The answer is accurate and concise; only the mode-token convention appears off in the provided text._

### `line-motions` — How do I jump to the end of a line?

- Related duplicates `$`, which is already the command explained in the answer.

**Warning:** related_quality
_The command behavior is accurate for both Vim and kindaVim, but the related list should avoid repeating the main command._

### `line-motions` — How do I go to the bottom of the document?

- `G` in Vim can take a count to go to a specific line, so `G — go to line 10` is inaccurate as written.
- Related duplicates the command discussed in the answer (`G`).

**Failing:** related_quality
**Warning:** vim_accuracy, kindavim_accuracy
_The core answer is fine, but the Related list contains both a duplicate and an inaccurate summary for `G`._

### `line-motions` — How do I jump to a specific line number?

- Mentions counted `G` behavior, but kindaVim support corpus only lists bare `G`, not `{count}G` specifically.

**Warning:** kindavim_accuracy
_Stock Vim behavior is correct, but the supported corpus does not explicitly include counted `G`._

### `line-motions` — What does `G` without a count do?

- `{count}G` is not wrapped in backticks.
- Related includes placeholder-style `{count}G`, which is not a canonical support-corpus command token.
- Related references a counted form not present in the support corpus.

**Failing:** xref_validity
**Warning:** token_conventions, related_quality
_The answer itself is correct, but formatting and related-command validity are off._

### `line-motions` — Can I use these with operators?

- 'The same operator+motion grammar works everywhere' is overly broad; Vim has exceptions and unsupported combinations.
- Answer is 4 sentences, slightly over the preferred 1–3 sentence limit.

**Warning:** vim_accuracy, conciseness
_The examples are good, but the sweeping final claim is too absolute and the entry is slightly long._

### `macros` — How do I record a macro in kindaVim?

- `terminalVim` is 4 sentences, exceeding the preferred 1–3 sentence limit.

**Warning:** conciseness
_Factually solid, but the terminalVim explanation is a bit longer than the target style._

### `macros` — Why doesn't `q` start recording in kindaVim?

- Claim that `q` is 'unbound' and 'has no effect' is not supported by the provided kindaVim corpus, which only says macros are unsupported.
- `terminalVim` includes a causal explanation about macOS text fields that is outside the authoritative Vim reference.
- Vim description of `q` recording is correct, but extra rationale is unsourced.

**Warning:** vim_accuracy, kindavim_accuracy, terminalvim_accuracy
_The core unsupported claim is right, but both the answer and terminalVim add implementation details not supported by the sources._

### `macros` — Can I chain commands without a macro?

- `<Esc>` should be wrapped in backticks as `` `Esc` `` per the house style, not written bare inside a command example.

**Failing:** token_conventions
_Substantively good, but it violates the token-formatting convention for `Esc`._

### `macros` — How do I record and replay a find-and-replace across a file?

- `terminalVim` macro example uses `cgn`, which kindaVim doesn't support but stock Vim does; description is broadly correct but implies replaying `@a` can run 'across a file' without caveats.

**Warning:** terminalvim_accuracy
_Unsupported status and workaround are correct, with a slightly oversimplified stock-Vim macro example._

### `marks` — How do I set a mark in kindaVim?

- Claim that `m` is 'unbound' is not established by the support corpus; only marks are unsupported.
- Related repeats the exact command from the answer, which is weak cross-linking.

**Warning:** kindavim_accuracy, related_quality
_Mostly correct, but it adds an unsupported detail about `m` being unbound._

### `marks` — How do I jump back to a mark?

- Answer calls `'` and `` ` `` 'operators', but in Vim they are mark-jump commands/motions, not operators.
- The backtick token is awkwardly escaped as `\``; token formatting is questionable even if intended to render a backtick command.

**Warning:** vim_accuracy, token_conventions
_The unsupported message is basically right, but it mislabels mark jumps as operators and the backtick token formatting is shaky._

### `marks` — Do automatic marks like `'.` work?

- Related summary 'restores position' is imprecise; undo restores text changes and may restore cursor/marks indirectly, but `u` is not an automatic-mark feature.

**Warning:** related_quality
_The unsupported claim and stock Vim mark descriptions are accurate, but the related item is a bit misleading._

### `named-registers` — Why doesn't `"ay` do anything?

- Claim that `"a` is interpreted as pending input is speculative about kindaVim parsing behavior rather than guaranteed by the support corpus.
- `"ay` in stock Vim yanks the current line because doubled operators act linewise; the terminalVim text is acceptable but slightly underspecified.

**Warning:** vim_accuracy
_The unsupported status is right, but the answer speculates about exact parser behavior that the corpus does not establish._

### `named-registers` — How do I access the last yank after I've deleted something?

- Answer begins with stock Vim behavior instead of first stating unsupported status explicitly, which weakens unsupported framing.

**Warning:** unsupported_consistency
_Facts are accurate, though the answer should more directly foreground that numbered registers are unsupported in kindaVim._

### `named-registers` — How do I use the system clipboard from kindaVim?

- `{{insert}}` is referenced while recommending host-app clipboard paste, but the support corpus does not establish mode-specific support for `Cmd+V` only in insert mode.

**Warning:** kindavim_accuracy
_The unsupported clipboard claim is correct, but the mode-specific paste wording is a bit more specific than the corpus supports._

### `named-registers` — Can I see what's in the register?

- Answer says kindaVim doesn't expose register contents, but the support corpus only explicitly marks named/register commands unsupported, not register visibility as a general product claim.

**Warning:** kindavim_accuracy
_The unsupported entry is mostly solid, with accurate stock Vim details for `:reg` and acceptable related links._

### `replace-characters` — How do I replace a single character?

- Related mentions `R` as 'limited in kindaVim', but `R` is supported in the corpus and 'limited' is unsupported extra nuance not grounded in the support list.

**Warning:** kindavim_accuracy, related_quality
_The main answer is accurate, but the related note adds unsupported qualification about `R`._

### `replace-characters` — Can `r` replace multiple characters at once?

- Related duplicates `cw`, which already appears in the answer.

**Warning:** related_quality
_The answer is accurate, but the related list repeats a command already discussed._

### `replace-characters` — How do I replace a whole word?

- `cw` is described as deleting 'the word', but in Vim it changes from the cursor position to the end of the word, with the special `cw`/`ce` behavior on non-blank text.

**Warning:** vim_accuracy
_Good practical guidance, but the `cw` description is slightly over-broad compared with Vim's exact behavior._

### `replace-characters` — How do I replace the character under the cursor with a newline?

- In stock Vim, replacing with a line break via `r` is with `<CR>` or `<NL>`; saying 'Enter' is imprecise in canonical command notation.
- `Enter` in the answer is not backtick-wrapped.

**Failing:** token_conventions
**Warning:** vim_accuracy, kindavim_accuracy
_The behavior is basically right, but the key notation should use backticked `<CR>` rather than plain Enter._

### `search` — How do I search for text?

- `Enter` is not backtick-wrapped.
- Uses 'Press' for the multi-key sequence '/... Enter'; convention says 'Type' for multi-character sequences.
- Related includes `n`, which is already mentioned in the answer.

**Failing:** token_conventions
**Warning:** verb_choice, related_quality
_The search behavior is correct, but notation and related-command hygiene need cleanup._

### `search` — How do I search backward?

- `Enter` in the answer is not backtick-wrapped
- Uses "press Enter" instead of `Enter` with backticks
- Related entry omits backticks around `N`

**Warning:** token_conventions, verb_choice
_Factually solid, but token formatting and verb phrasing are slightly inconsistent with the house style._

### `search` — How do I search for the word my cursor is on?

- Uses `/word<Enter>` and `?word<Enter>` instead of backtick-wrapped keys/sequence tokens
- Uses "Type"-style multi-key examples without the preferred verb
- Related field omits backticks around `/` and `?`

**Warning:** token_conventions, verb_choice
_The unsupported claim and stock-Vim explanation are accurate, with only style issues around tokenization and phrasing._

### `search` — What's the difference between `/` and `f`?

- Related field omits backticks around `n`

**Warning:** token_conventions
_The explanation is correct for both Vim and kindaVim, with only a minor related-token formatting issue._

### `search` — Is search case-sensitive?

- Claims default case-sensitivity without support-corpus evidence for kindaVim search settings
- Stock Vim case-sensitivity depends on options rather than an unconditional default-yes claim
- Related field omits backticks around `/`

**Warning:** vim_accuracy, kindavim_accuracy, token_conventions
_The general idea may be reasonable, but the unconditional case-sensitivity claim is not well-supported from the provided authorities._

### `search` — Does search wrap around the document?

- Claims search wraps in stock Vim without noting this depends on the `wrapscan` option
- Describes kindaVim as following host-app search mechanics, which is not established by the support corpus
- Quoted stock-Vim message is implementation detail tied to wrap behavior

**Warning:** vim_accuracy, kindavim_accuracy, token_conventions
_This answer overstates wraparound as unconditional and speculates about kindaVim behavior beyond the support list._

### `search` — How do I delete to the next match?

- Answer uses `/pattern<Enter>` without backticks around `<Enter>` separately.
- Related commands `c/pattern` and `y/pattern` are not listed in the support corpus as canonical commands.

**Failing:** xref_validity
**Warning:** token_conventions, related_quality
_The stock Vim description is fine and kindaVim supports `/` plus `d`, but the related entries use non-corpus command forms._

### `search` — How do I cancel a search I'm typing?

- `Ctrl-C` during search input is plausible but not supported by the provided Vim reference excerpt.
- kindaVim support corpus lists `Esc` and `Enter`, but not `Ctrl-C` as a supported search-cancel key.

**Warning:** vim_accuracy, kindavim_accuracy
_This is likely correct behavior, but `Ctrl-C` is not grounded in the provided references or support list._

### `splits-and-tabs` — How do I switch between files in kindaVim?

- Related token should be `Cmd+\`` not `Cmd+\`.
- Related summary is inaccurate for the intended shortcut.
- Related command `Cmd+\` is not in the support corpus.

**Failing:** token_conventions, related_quality, xref_validity
_The unsupported claim and terminal Vim buffer info are fine, but the Related entry is malformed and invalid._

### `splits-and-tabs` — How do I view two files side by side in kindaVim?

- Terminal Vim field mentions `:diffsplit`, which is not covered in the provided Vim reference excerpt.
- Related item `macOS tile windows` is not a command entry from the support corpus.
- Related summary is not in the usual `command — summary` format.

**Failing:** related_quality, xref_validity
**Warning:** vim_accuracy, terminalvim_accuracy, token_conventions
_The non-support claim is correct, but the related field is off-format and not corpus-backed._

### `splits-and-tabs` — How do I close a split or tab?

- :qa closes all windows/tab pages and exits Vim, not just 'all splits'.
- `:` commands in terminalVim are not wrapped in backticks.

**Warning:** terminalvim_accuracy
_The kindaVim claim is correct, but the stock Vim description is slightly imprecise about `:qa`._

### `substitute-shortcuts` — How do I repeat a find-and-replace in kindaVim?

- Related includes `cw`, which is already mentioned in the answer, so it duplicates an answer command.

**Warning:** related_quality
_The unsupported explanation is solid, but the related list repeats a command already used in the answer._

### `substitute-shortcuts` — What's the difference between `.` and `&`?

- Uses plain 'CHANGE' instead of a clearer Vim term like 'change' or backticked commands; stylistically odd though not factually wrong.

**Warning:** token_conventions
_This clearly distinguishes `.` from `&` and correctly marks `&` as unsupported in kindaVim._

### `substitute-shortcuts` — Can I repeat a substitution across multiple lines in kindaVim?

- `cw<replacement><Esc>` is not valid Vim notation; replacement text should be described as typed text after `cw`.
- `n.` is presented as a single token even though it is two commands in sequence.
- Uses raw `/pattern<Enter>` notation rather than backticking command tokens consistently.

**Warning:** vim_accuracy, token_conventions, verb_choice
_The main unsupported claim is correct, but the workaround notation is sloppy and partially non-canonical._

### `text-objects` — How do I delete the word the cursor is on?

- `diw` operates on a Vim word, not 'just letters'; words can include digits/underscores or other non-blank runs depending on context/options.

**Warning:** vim_accuracy
_Good supported guidance overall, but it slightly oversimplifies what counts as a Vim word._

### `text-objects` — How do I yank a quoted string?

- Uses escaped quotes inside backticks (`yi"`, `ya"`, `di"`, `ci"`) instead of canonical command tokens like `yi"` rendered from source as `yi"`; author convention expects backtick-wrapped keys, not backslash-escaped presentation.
- Related summaries are vague/incomplete ('delete inside quotes', 'change inside') and `di"` is not present as a corpus token; supported corpus has `di"`/`ci"` only conceptually via actual `di"` text in source, but related command token as written may not match corpus exactly.

**Failing:** token_conventions, related_quality, xref_validity
_The behavior is correct, but command token formatting and related references are not in clean canonical form._

### `text-objects` — How do I delete everything inside parentheses?

- Related includes `dab`, which is already mentioned in the answer; related commands should avoid duplication.

**Warning:** related_quality
_The command description is accurate, but the related section repeats a command already explained._

### `text-objects` — Why `dib` instead of `di(`?

- Saying raw `(` / `{` forms 'aren't independently verified' understates that kindaVim marks forms like `di(` as unsupported.

**Warning:** kindavim_accuracy
_Stock Vim description is correct, but the kindaVim-specific wording should more clearly state the unsupported raw forms._

### `text-objects` — How do I change the contents of a string without removing the quotes?

- Key token should be written as ``ci"`` in backticks per the author convention; current escaping breaks the token presentation.

**Failing:** token_conventions
_The behavior is correct, but the command token formatting is off._

### `text-objects` — How do I change everything inside braces?

- Related includes `ciB`, which is already the main command in the answer; related commands should not duplicate answer commands.

**Warning:** related_quality
_Accurate explanation, but the related list is redundant._

### `text-objects` — How do I select text using a text object?

- `viw` does not literally 'enter visual, then selects' as two separate steps the user performs here; it's a single normal-mode command sequence.
- Saying 'In {{visual}} mode, apply a text object' is slightly misleading for examples like `viw`, which start from {{normal}} and enter Visual as part of the sequence.

**Warning:** vim_accuracy
_The examples are supported, but the mode wording is a bit misleading relative to how `viw` is entered._

### `text-objects` — Can I use a count with text objects?

- Claim about kindaVim count support being 'uneven' is not grounded in the support corpus, which doesn't specify partial/uneven count behavior for text objects.

**Warning:** kindavim_accuracy
_The stock-Vim count explanation is accurate, but the kindaVim-specific caveat is unsupported by the corpus._

### `text-objects` — How do I delete to the matching bracket?

- Says kindaVim 'doesn't verify `%` specifically' instead of clearly stating whether `d%` itself is supported; support corpus does not list `%` or `d%`.
- Related duplicates commands already mentioned in the answer.

**Warning:** kindavim_accuracy, related_quality
_Stock Vim description of `d%` is correct, but the kindaVim support claim is hedged rather than directly aligned to the corpus._

### `text-objects` — My cursor is after the word. Will `diw` still work?

- `diw` on whitespace behavior is oversimplified; stock Vim's inner-word object has specific whitespace/object rules rather than a generic 'nearest word' rule.
- 'If the cursor is truly past all content, nothing matches' is vague and not a precise Vim reference description.

**Warning:** vim_accuracy
_This is directionally useful, but the stock Vim behavior is described too loosely to fully trust._

### `text-objects` — Which text object is the most useful one to learn first?

- Related summary for `yiw` is imprecise; stock Vim treats `iw` as "inner word" text object, so "yank inner word" is informal rather than the app's usual command-summary style.

**Warning:** related_quality
_The answer is accurate and concise, but one related summary is slightly loose in terminology._

### `undo-redo` — How do I undo multiple changes at once?

- The answer claims counted undo (`3u`) for kindaVim, but the support corpus only explicitly lists `u`, not counted forms.
- Related includes `U`, which is not present in the kindaVim support corpus.
- Related summary for `U` describes stock Vim only, but related links should point to corpus-listed commands.

**Failing:** related_quality, xref_validity
**Warning:** kindavim_accuracy
_The Vim part is right, but the kindaVim support claim and related command reference are not fully grounded in the corpus._

### `undo-redo` — Can I undo past the start of an edit session?

- Answer discusses kindaVim/host-app behavior but does not describe stock Vim behavior for supported entry
- Related includes non-corpus phrase `host app undo (Cmd-Z equivalent)`

**Warning:** vim_accuracy, related_quality, xref_validity
_kindaVim support is fine, but supported entries are expected to also reflect stock Vim behavior and the related field mixes in a non-command reference._

### `visual-mode` — What's the difference between `v` and `V`?

- Uses plain `normal` instead of double-braced mode token in related summary

**Warning:** token_conventions
_The command behavior is correct, but the related summary should use `{{normal}}` rather than plain text._

### `visual-mode` — How do I delete a visual selection?

- `x` in Visual mode deletes highlighted text in Vim, but kindaVim support corpus does not list Visual `x` support
- Claim about default register is broader than the support corpus explicitly documents

**Warning:** vim_accuracy, xref_validity
_The main behavior is plausible, but it leans on Visual `x` and register details that are not clearly covered by the kindaVim corpus._

### `visual-mode` — Can I extend a visual selection word by word?

- Uses plain `visual mode` instead of double-braced `{{visual}}`

**Warning:** token_conventions
_The motion facts are correct, but the mode mention should follow the required double-brace convention._

### `visual-mode` — How do I select text inside parens in visual mode?

- Related includes `vib`, which is already used in the answer.

**Warning:** related_quality
_The answer accurately describes supported `vib`/`vab` behavior and follows the formatting conventions._

### `visual-mode` — How do I cancel a visual selection?

- 'The cursor ends up at the last position of the selection' is imprecise; Vim documents `Esc` stops Visual mode but this cursor claim is not clearly supported here
- Related `v — re-enter visual` summary is a bit inaccurate because `v` starts Visual mode rather than re-entering the previous selection

**Warning:** vim_accuracy, related_quality
_Mostly fine, but the cursor-position claim is too specific and the related summary is slightly misleading._

### `visual-mode` — Does visual mode support counts?

- Uses 'visual mode' instead of `{{visual}}`.
- `v3w` is not listed in the support corpus, so claiming this specific supported behavior is not fully grounded.
- Uses `v3w`, which is a multi-key sequence; prefer 'Type' phrasing if giving an action instruction.

**Failing:** token_conventions
**Warning:** kindavim_accuracy, verb_choice
_The Vim statement is fine, but the mode formatting and support grounding are weaker than required._

### `visual-mode` — How do I select from here to the end of the line?

- `v`, then `$` should use 'Type' rather than 'Press' for a multi-step sequence
- Related summary `V — linewise from the start` is inaccurate because `V` selects whole lines, not specifically 'from the start'

**Warning:** vim_accuracy, verb_choice, related_quality
_Core behavior is acceptable, but phrasing and the related summary need tightening._

### `word-motions` — How do I jump forward by a word?

- Uses 'Press' while also presenting multi-character sequence example `3w`; preferred convention would distinguish `w` from typing `3w`

**Warning:** verb_choice
_Accurate and supported overall, with only a minor verb-choice issue._

### `word-motions` — How do I jump back a word?

- Uses 'Press' while also presenting multi-character sequence example `2b`; preferred convention would distinguish `b` from typing `2b`

**Warning:** verb_choice
_Accurate and supported, with just a minor verb-choice nit._

### `word-motions` — What's the difference between `w` and `e`?

- Related omits backtick formatting for `w` in 'WORD version of w' summary.

**Warning:** related_quality
_Core explanation is correct, but related formatting is slightly inconsistent with token conventions._

### `word-motions` — How do counts work with word motions?

- Related references unsupported/absent command form `d3w` instead of canonical supported token(s) like `dw`.

**Warning:** related_quality
_The count explanation is accurate, but the related entry uses a non-corpus command form._

### `word-motions` — How do I delete a word using word motions?

- `dw` does not always 'eat trailing space'; at end-of-line/operator special cases make this oversimplified for stock Vim.
- `de` description is broadly right but simplified; edge cases are omitted.

**Warning:** vim_accuracy
_kindaVim support is fine, but the stock Vim description is a bit too absolute about whitespace behavior._

### `word-motions` — How do I move to the start of the line?

- Related duplicates commands already named in the answer (`0`, `^`).

**Warning:** related_quality
_The answer is accurate, but related commands should avoid duplicating commands already used in the answer._

### `word-motions` — Why does `w` sometimes seem to skip characters?

- Says `w` skips whitespace, but in Vim `w` also moves across punctuation/non-blank word boundaries, not just spaces.
- Related summary for `W` is oversimplified; `W` moves by WORDs separated by whitespace, not merely 'skips more'.

**Warning:** vim_accuracy, related_quality
_Mostly fine for beginner intuition, but it overstates whitespace as the only reason `w` seems to skip._

### `word-motions` — Which word motion should I learn first?

- Related repeats commands already named in the answer (`w`, `b`, `e`).

**Warning:** related_quality
_The answer is sound, but the related list duplicates commands already discussed._

### `word-motions` — What's the WORD motion useful for?

- Calling `my_variable.someMethod(arg)` 'ONE WORD to `W`' is misleading; `W` moves by whitespace-delimited WORDs, but exact segmentation depends on cursor position and motion semantics.
- Related uses non-canonical combined token `w / b / e`, which is not a corpus command.
- Related entry duplicates commands already discussed in the answer.

**Failing:** xref_validity
**Warning:** vim_accuracy, related_quality
_Good intuition about WORD motions, but one example is too absolute and the related field is malformed._

### `yank-and-put` — How do I copy text in kindaVim?

- `y` is an operator and does not complete after a single keypress; 'Press `y` ... followed by a motion' should use 'Type' for the multi-key command form.
- Saying the text lives in the default register until your next yank or delete is imprecise in Vim; the unnamed register updates on delete/change, but numbered and other registers also exist.
- kindaVim explicitly marks advanced registers as unsupported, so referring broadly to 'the default register' is acceptable but slightly underspecified.
- Answer is 4 sentences, exceeding the preferred 1-3 sentence target.
- Related includes `yy`, which is already used in the answer.

**Warning:** vim_accuracy, kindavim_accuracy, verb_choice, related_quality, conciseness
_Core behavior is right, but the wording around registers and the command verb choice could be tighter._

### `yank-and-put` — What's the difference between `p` and `P`?

- Related repeats commands already named in the answer (`y`, `yy`).

**Warning:** related_quality
_Accurate description of characterwise versus linewise put behavior._

### `yank-and-put` — How do I copy a whole line?

- Related summary says `p` 'paste below'; more precisely `p` puts linewise text below the current line, but characterwise text after the cursor.

**Warning:** related_quality
_Main answer is accurate and supported, with only a slightly overbroad Related summary for `p`._

### `yank-and-put` — Why does `p` sometimes paste on a new line?

- Related summary for `y` is imprecise; `y` is an operator, not specifically a characterwise yank.

**Warning:** related_quality
_The answer is accurate and concise, but the related item oversimplifies what `y` means._

### `yank-and-put` — How do I copy a word?

- `yaw` does not always include trailing space specifically; `a` text objects include surrounding whitespace, which may be before instead of after depending on context.
- Related summary for `yaw` should avoid claiming only trailing space.

**Warning:** vim_accuracy, related_quality
_The core guidance is good, but the whitespace behavior of `yaw` is stated too narrowly._

### `yank-and-put` — How do I paste from the system clipboard?

- Answer says kindaVim doesn't support `"+`, but `isUnsupported` is false and the answer is framed as supported guidance rather than a clear unsupported feature entry.

**Warning:** kindavim_accuracy, unsupported_consistency
_The guidance is practical and factually fine, but it mixes unsupported-feature language into a supported entry._

### `yank-and-put` — How do I copy from where I am to the end of the line?

- `Y` is not just a modern-Vim alias; in Vim it is a synonym for `yy`, so the wording is historically/editorially imprecise.

**Warning:** vim_accuracy
_The main command `y$` is correct, but the aside about `Y` should be stated more precisely._

### `yank-and-put` — How do I yank multiple lines?

- `yG` is mentioned in the answer but is not present in the kindaVim support corpus.
- Related command `dG` duplicates a command family already referenced in the answer and its summary is vague.

**Failing:** xref_validity
**Warning:** vim_accuracy, kindavim_accuracy, related_quality
_The Vim claim is fine, but it references `yG`, which is not listed as supported by kindaVim._

### `yank-and-put` — Where does the yanked text go when I yank something new?

- In Vim, the unnamed/default register is updated by more than just yank or delete; change and substitute commands also write to it.
- Saying the default register holds only the most recent yank or delete is imprecise for stock Vim.
- Related entry `"0` is valid but its summary is oversimplified; Vim's `"0` holds the most recent yank unless an explicit register was used, not simply a generic "yank-only register".
- Related text uses `"0` rather than the preferred backtick-wrapped command token formatting.

**Warning:** vim_accuracy, token_conventions, related_quality
_Accurate enough for kindaVim's default-register behavior, but the stock-Vim wording is too simplified and the related formatting/summary need tightening._

## Per-topic flagged counts

- `case-toggle` (supported): 2/6 flagged
- `change-operator` (supported): 5/10 flagged
- `delete-operator` (supported): 8/10 flagged
- `delete-word` (supported): 5/9 flagged
- `dot-repeat` (supported): 3/8 flagged
- `ex-commands` (unsupported): 6/10 flagged
- `find-character` (supported): 1/8 flagged
- `folds` (unsupported): 4/5 flagged
- `insert-mode-entry` (supported): 5/12 flagged
- `join-lines` (supported): 4/5 flagged
- `jumplist` (unsupported): 4/4 flagged
- `line-motions` (supported): 6/10 flagged
- `macros` (unsupported): 4/5 flagged
- `marks` (unsupported): 3/5 flagged
- `named-registers` (unsupported): 4/6 flagged
- `replace-characters` (supported): 4/6 flagged
- `search` (supported): 8/11 flagged
- `splits-and-tabs` (unsupported): 3/6 flagged
- `substitute-shortcuts` (unsupported): 3/4 flagged
- `text-objects` (supported): 11/14 flagged
- `undo-redo` (supported): 2/7 flagged
- `visual-mode` (supported): 7/12 flagged
- `word-motions` (supported): 9/15 flagged
- `yank-and-put` (supported): 9/12 flagged